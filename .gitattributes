# Normalize line endings to LF in the repository for consistent cross-platform
# checkouts and clean diffs (CI runs on Linux).
* text=auto eol=lf

# Treat binary assets as binary.
*.png binary
*.jpg binary
*.ico binary
